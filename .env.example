# Modèle de configuration — copier vers `.env.local` puis renseigner les valeurs.
# `.env.local` est ignoré par git (ne jamais committer de secret ni d'URL interne).
# Ce fichier-ci NE doit contenir AUCUNE valeur réelle : c'est un template.

# === Dolibarr Maître ===
# L'application appelle réellement le Dolibarr Maître : l'URL et la clé ci-dessous
# sont requises pour la vérification de sous-domaine et le provisioning.

# URL de base de l'API REST du Maître (module « API/Web services REST » activé).
# Utilisée en LECTURE : unicité du sous-domaine + suivi du déploiement (GET /contracts).
DOLIBARR_API_URL=https://votre-dolibarr-maitre.example.com/api/index.php

# Jeton DOLAPIKEY d'un utilisateur habilité (Tiers + Contrats + Produits + objets
# Sell Your SaaS). À générer dans Dolibarr : fiche Utilisateur → onglet « Clé API ».
DOLIBARR_API_KEY=

# URL du handler public de provisioning SYS (portail « myaccount »). C'est lui qui
# crée tiers + contrat + instance et DÉCLENCHE le déploiement (pas l'API REST).
# Sans cette URL, le provisioning (flux réel) échoue explicitement.
SELLYOURSAAS_REGISTER_URL=https://myaccount.votre-domaine.example.com/register_instance.php

# URL de base du portail « myaccount » : sert de Referer pour register_instance.php,
# qui REFUSE un Referer vide (contrôle anti-bot). Défaut : https://myaccount.<INSTANCE_DOMAIN>.
SELLYOURSAAS_ACCOUNT_URL=https://myaccount.votre-domaine.example.com

# Domaine (schéma de sous-domaine) des instances : URL = <sous-domaine>.<INSTANCE_DOMAIN>
# Côté Sell Your SaaS c'est le sous-domaine de déploiement (ex. with1.pichinov.fr).
INSTANCE_DOMAIN=with1.pichinov.fr

# === MailJet (e-mails transactionnels : contact + bienvenue) ===
# Clés API MailJet (Compte → API Key Management). Requises : sans elles, l'envoi
# d'e-mails échoue explicitement (les envois sont visibles dans le dashboard MailJet).
MJ_APIKEY_PUBLIC=
MJ_APIKEY_PRIVATE=

# Adresse expéditrice des e-mails sortants (doit être validée côté MailJet).
MAIL_FROM=contact@pichinov.com